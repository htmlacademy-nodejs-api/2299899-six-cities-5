export type RestSchema = {
  PORT: number;
  SALT: string;
  DB_HOST: string;
  DB_USER: string;
  DB_PASSWORD: string;
  DB_PORT: string;
  DB_NAME: string;
  DB_UI_PORT: string;
  DB_UI_USER: string;
  DB_UI_PASSWORD: string;
};
